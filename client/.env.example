VITE_API_BASE_URL=

# Firebase API Key (only sensitive value that needs to be in env)
VITE_FIREBASE_API_KEY=

# Firebase Emulator (uncomment to use local emulator instead of production)
VITE_USE_FIREBASE_EMULATOR=false
